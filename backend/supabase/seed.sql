-- Seed data for local development

-- Note: Test users should be created through Supabase Auth signup
-- The trigger will automatically create profiles when users sign up
-- This file can be used to insert additional data after user creation

-- Example seed data structure:
-- INSERT INTO public.profiles (id, email, username, full_name, bio, is_active, is_superuser) VALUES
--     ('user-uuid-from-auth', 'admin@teamai.local', 'admin', 'System Administrator', 'Local development admin user', true, true)
-- ON CONFLICT (id) DO NOTHING;

-- For now, this file is empty to prevent foreign key constraint errors
-- Test users will be created through the normal signup process
